#!/usr/bin/env bash
set -euo pipefail

ROOT="/etc/nixos"
HOST="$(hostname)"
SESSION_FILE="$ROOT/docs/SESJA.md"

DRY=0
FORCE=0
PANIC=0

for arg in "$@"; do
  case "$arg" in
  --dry) DRY=1 ;;
  --force) FORCE=1 ;;
  --panic) PANIC=1 ;;
  esac
done

RED="\033[31m"
YELLOW="\033[33m"
GREEN="\033[32m"
RESET="\033[0m"

fail() {
  echo -e "${RED}❌ $1${RESET}"
  exit 1
}

warn() {
  echo -e "${YELLOW}⚠️  $1${RESET}"
}

ok() {
  echo -e "${GREEN}✅ $1${RESET}"
}

header() {
  echo
  echo "=============================="
  echo "🛡  NSS SAFETY CHECK"
  echo "=============================="
  echo
}
header

if [[ "$PANIC" -eq 1 ]]; then
  echo "🧯 PANIC MODE: rollback"
  sudo nixos-rebuild switch --rollback ||
    fail "Rollback failed"
  ok "Rollback OK"
  exit 0
fi

[[ "$DRY" -eq 1 ]] && warn "DRY RUN: brak zmian w systemie"

cd "$ROOT"

# 1. repo clean (tracked + untracked)
if [[ -n "$(git status --porcelain)" ]]; then
  if [[ "$FORCE" -eq 1 ]]; then
    warn "FORCE: repo brudne, kontynuuję"
  else
    fail "Repo /etc/nixos jest brudne (tracked lub untracked)"
  fi
fi
ok "Repo clean"

# 2. flake check
echo "🔍 nix flake check…"
nix flake check "$ROOT" || fail "flake check failed"
ok "Flake OK"

CRITICAL_PATHS=(
  "nixos/configuration.nix"
  "flake.nix"
  "modules/boot.nix"
  "modules/graphics.nix"
)

CRITICAL_HIT=0

for path in "${CRITICAL_PATHS[@]}"; do
  if git diff --name-only | grep -q "^$path"; then
    CRITICAL_HIT=1
  fi
done

if [[ "$CRITICAL_HIT" -eq 1 ]]; then
  warn "Zmiany dotykają KRYTYCZNYCH PLIKÓW"
  RISK="HIGH"
else
  RISK="NORMAL"
fi

echo
echo "🧠 Decision gate"
echo "Ryzyko: $RISK"
echo
echo "Zmodyfikowane pliki:"
git diff --name-only
echo

echo "[A] build only"
echo "[B] build + switch"
echo "[C] build + switch + commit"
echo "[D] abort (domyślne)"
echo

read -r -p "Wybór: " CHOICE
CHOICE="${CHOICE:-D}"

case "$CHOICE" in
A | a) MODE="build" ;;
B | b) MODE="switch" ;;
C | c) MODE="commit" ;;
*)
  fail "Abort by user"
  ;;
esac

ok "Wybrany tryb: $MODE"

echo
echo "🏗  BUILD: nix build (system)"
if [[ "$DRY" -eq 1 ]]; then
  warn "DRY: pomijam build"
else
  nix build "$ROOT#nixosConfigurations.$HOST.config.system.build.toplevel" ||
    fail "Build failed"
  ok "Build OK"
fi

if [[ "$MODE" == "switch" || "$MODE" == "commit" ]]; then
  echo
  echo "🔁 SWITCH: nixos-rebuild switch"
  if [[ "$DRY" -eq 1 ]]; then
    warn "DRY: pomijam switch"
  else
    sudo nixos-rebuild switch --flake "$ROOT#$HOST" ||
      fail "Switch failed"
    ok "Switch OK"
  fi
fi

read -r -p "Kontynuować? (wpisz 'zamykamy' aby przerwać): " LAST
if [[ "$LAST" == "zamykamy" ]]; then
  fail "Przerwano hasłem awaryjnym"
fi

ok "Bezpiecznik przeszedł"
